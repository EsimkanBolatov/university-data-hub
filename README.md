# 🎓 University DataHub - Backend API

**Комплексная платформа для каталогизации университетов Казахстана**

## 🌟 Особенности

- ✅ Полная информация о 3+ университетах Алматы
- ✅ Детальные карточки с программами, факультетами, грантами
- ✅ Умный поиск и фильтрация
- ✅ Сравнение университетов
- ✅ Система избранного для пользователей
- ✅ JWT авторизация
- ✅ Админ-панель через API

## 🛠 Технологический стек

- **Язык**: Python 3.12
- **Фреймворк**: FastAPI (Асинхронный)
- **База данных**: PostgreSQL + asyncpg
- **ORM**: SQLAlchemy 2.0
- **Миграции**: Alembic
- **Безопасность**: JWT, BCrypt
- **Инфраструктура**: Docker & Docker Compose

## ⚡ Быстрый старт

### 1. Клонирование и настройка

```bash
git clone <your-repo-url>
cd University_DataHub_Backend
```

Создай файл `.env`:
```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=university_db
DB_HOST=db
DB_PORT=5432

SECRET_KEY=your_super_secret_key_change_me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### 2. Запуск через Docker

```bash
# Запустить контейнеры
docker-compose up --build -d

# Применить миграции
docker-compose exec backend alembic upgrade head

# Загрузить тестовые данные
docker-compose exec backend python -m scripts.seed_data
```

### 3. Проверка работы

Открой: **http://localhost:8000/docs**

Войди с тестовым админом:
- Email: `admin@university.kz`
- Password: `admin123`

## 📚 Структура базы данных

### Основные таблицы:

1. **users** - Пользователи (студенты, админы)
2. **universities** - Университеты (40+ полей)
3. **faculties** - Факультеты
4. **departments** - Кафедры
5. **programs** - Образовательные программы
6. **grants** - Гранты и стипендии
7. **dormitories** - Общежития
8. **partnerships** - Международное сотрудничество
9. **favorites** - Избранные университеты
10. **admissions** - Условия поступления

## 🔑 Основные эндпоинты

### 🏛 Университеты

```http
GET    /universities/                    # Список с фильтрами
GET    /universities/{id}                # Детальная информация
POST   /universities/                    # Создать (админ)
PATCH  /universities/{id}                # Обновить (админ)
DELETE /universities/{id}                # Удалить (админ)
POST   /universities/compare             # Сравнение
```

### 📖 Программы

```http
POST   /universities/{id}/programs       # Добавить программу
GET    /universities/programs/search     # Поиск программ
```

### 🏫 Факультеты, гранты, общежития

```http
POST   /universities/{id}/faculties      # Добавить факультет
POST   /universities/{id}/grants         # Добавить грант
GET    /universities/{id}/grants         # Список грантов
POST   /universities/{id}/dormitories    # Добавить общежитие
```

### ⭐ Избранное

```http
POST   /universities/{id}/favorite       # Добавить
DELETE /universities/{id}/favorite       # Удалить
GET    /universities/favorites/my        # Мои избранные
```

### 🔐 Авторизация

```http
POST   /auth/register                    # Регистрация
POST   /auth/login                       # Вход
GET    /auth/me                          # Текущий пользователь
```

## 🔍 Примеры использования

### Поиск университетов

```bash
# С общежитием в Алматы
curl "http://localhost:8000/universities/?city=Алматы&has_dormitory=true"

# По названию и рейтингу
curl "http://localhost:8000/universities/?query=Satbayev&min_rating=4.0"
```

### Поиск программ

```bash
# Бакалавриат до 1.5 млн тенге
curl "http://localhost:8000/universities/programs/search?degree=bachelor&max_price=1500000"

# По городу и степени
curl "http://localhost:8000/universities/programs/search?city=Алматы&degree=master"
```

### Сравнение университетов

```bash
curl -X POST "http://localhost:8000/universities/compare" \
  -H "Content-Type: application/json" \
  -d '{
    "university_ids": [1, 2, 3]
  }'
```

## 📊 Что включено в тестовые данные

1. **Satbayev University**
   - 3 программы (бакалавриат + магистратура)
   - Факультет IT
   - Гранты
   - Общежитие
   - Международное партнерство

2. **KIMEP University**
   - 2 программы (BBA, MBA)
   - Престижный частный вуз

3. **AlmaU**
   - 2 программы (Менеджмент, Маркетинг)
   - Бизнес-образование

## 🚀 Следующие шаги для разработки

### Этап 2: Расширение функционала
- [ ] Добавить полнотекстовый поиск (PostgreSQL FTS)
- [ ] Добавить рекомендательную систему
- [ ] Создать эндпоинт для календаря поступления
- [ ] Добавить отзывы студентов
- [ ] Интеграция с картами (Google Maps)

### Этап 3: Парсинг данных
- [ ] Написать парсер для официальных сайтов вузов
- [ ] Автоматическое обновление данных
- [ ] ETL pipeline с Airflow

### Этап 4: Frontend
- [ ] React + Next.js
- [ ] Карточки университетов
- [ ] Интерактивные фильтры
- [ ] 3D-туры (iframe/WebGL)
- [ ] Сравнение (таблица)

## 🐛 Решение проблем

**База данных не поднимается:**
```bash
docker-compose down -v
docker-compose up --build
```

**Ошибка миграций:**
```bash
docker-compose exec backend alembic downgrade -1
docker-compose exec backend alembic upgrade head
```

**Пересоздать данные:**
```bash
docker-compose down -v
docker-compose up -d
docker-compose exec backend alembic upgrade head
docker-compose exec backend python -m scripts.seed_data
```

## 📁 Структура проекта

```
University_DataHub_Backend/
├── app/
│   ├── core/              # Конфигурация, безопасность
│   ├── db/                # Модели БД
│   ├── routers/           # API эндпоинты
│   ├── schemas/           # Pydantic схемы
│   ├── dependencies.py    # JWT авторизация
│   └── main.py            # Точка входа
├── scripts/
│   └── seed_data.py       # Загрузка тестовых данных
├── alembic/               # Миграции
├── .env                   # Переменные окружения
├── docker-compose.yml     # Docker конфигурация
└── requirements.txt       # Зависимости Python
```

## 👨‍💻 Для разработчиков

### Создание новой миграции

```bash
docker-compose exec backend alembic revision --autogenerate -m "описание изменений"
docker-compose exec backend alembic upgrade head
```

### Подключение к базе

```bash
docker-compose exec db psql -U postgres -d university_db
```

### Просмотр логов

```bash
docker-compose logs -f backend
```

## 📄 Лицензия

MIT License

## 🤝 Команда

Создано для хакатона ITU HACKATHON-2025

---

**⚡ Готово к хакатону! Запускай и побеждай! 🏆**
