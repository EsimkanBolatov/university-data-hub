# 🎓 University DataHub - Backend

**Backend API для каталога университетов и образовательных программ Казахстана**

## 🛠 Технологический стек

- **Язык**: Python 3.12
- **Фреймворк**: FastAPI (Асинхронный)
- **База данных**: PostgreSQL + asyncpg
- **ORM**: SQLAlchemy 2.0
- **Миграции**: Alembic
- **Безопасность**: JWT, BCrypt
- **Инфраструктура**: Docker & Docker Compose

## ⚡ Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd University_DataHub_Backend
```

### 2. Настройка окружения
Создай файл `.env` в корне проекта (уже есть шаблон выше)

### 3. Запуск через Docker (Рекомендуется)
```bash
docker-compose up --build
```

Приложение запустится на `http://localhost:8000`

### 4. Запуск без Docker (Локально)

#### Установка зависимостей:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

#### Запуск PostgreSQL локально:
```bash
# Убедись что PostgreSQL установлен и запущен
# Создай базу данных:
createdb university_db
```

#### Применение миграций:
```bash
alembic upgrade head
```

#### Запуск сервера:
```bash
uvicorn app.main:app --reload
```

## 📖 API Документация

После запуска открой:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔑 Основные эндпоинты

### Авторизация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход (получение JWT токена)
- `GET /auth/me` - Информация о текущем пользователе

### Университеты
- `GET /universities/` - Список университетов (с фильтрами)
- `GET /universities/{id}` - Детальная информация
- `POST /universities/` - Создать университет (админ)
- `POST /universities/{id}/programs` - Добавить программу (админ)
- `GET /universities/programs/search` - Поиск программ

## 📊 Структура базы данных

### Таблицы:
1. **users** - Пользователи системы
2. **universities** - Университеты
3. **programs** - Образовательные программы

## 🔐 Создание первого админа

После запуска зарегистрируйся через `/auth/register`, затем подключись к БД и измени роль:

```sql
UPDATE users SET role = 'admin' WHERE email = 'твой@email.com';
```

## 🚀 Следующие шаги

### Этап 2: Добавление данных
1. Создай скрипт для парсинга сайтов вузов (например, Satbayev University)
2. Или добавь данные вручную через Swagger UI

### Этап 3: Расширение функционала
- [ ] Добавить поиск с помощью PostgreSQL Full-Text Search
- [ ] Добавить эндпоинт сравнения вузов
- [ ] Добавить фильтры по грантам и общежитиям
- [ ] Добавить систему избранного для пользователей

## 🐛 Решение проблем

**Ошибка подключения к БД:**
```bash
docker-compose down -v
docker-compose up --build
```

**Ошибка миграций:**
```bash
docker-compose exec backend alembic upgrade head
```

## 📝 Примеры запросов

### Регистрация:
```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "full_name": "Тестовый Пользователь"
  }'
```

### Вход:
```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=test@example.com&password=password123"
```

### Получение списка вузов:
```bash
curl -X GET "http://localhost:8000/universities/?city=Алматы"
```

## 👥 Команда разработки

Создано для хакатона ITU HACKATHON-2025

## 📄 Лицензия

MIT License

````
University_DataHub_Backend/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py
│   │   └── security.py
│   ├── db/
│   │   ├── __init__.py
│   │   ├── database.py
│   │   └── models.py
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   └── universities.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── university.py
│   └── dependencies.py
├── alembic/
│   ├── versions/
│   └── env.py
├── .env
├── .gitignore
├── alembic.ini
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── README.md
````