# 🔄 Руководство по миграции на v2.0

## 📋 Что изменилось?

### Исправленные проблемы

| # | Проблема | Решение |
|---|----------|---------|
| 1 | AI не отвечает без данных | ✅ Добавлен веб-поиск |
| 2 | Недоработанный каталог | ✅ 10+ фильтров, сортировка, пагинация |
| 3 | Нет системы рейтинга | ✅ AI-powered рекомендации |
| 4 | Нет сравнения | ✅ Сравнение 2-5 вузов с AI анализом |
| 5 | Нет избранного | ✅ Полноценная система избранного |

---

## 🗂️ Новые/изменённые файлы

### ✨ Обязательно обновить:

1. **app/services/ai_service.py** (ПОЛНОСТЬЮ ЗАМЕНИТЬ)
   - Добавлен веб-поиск
   - Улучшенная синхронизация
   - Расширенные рекомендации

2. **app/routers/ai.py** (ПОЛНОСТЬЮ ЗАМЕНИТЬ)
   - Новые эндпоинты
   - Обновлённые схемы

3. **app/routers/catalog.py** (ПОЛНОСТЬЮ ЗАМЕНИТЬ)
   - Новый роутер с фильтрами

4. **app/routers/favorites.py** (СОЗДАТЬ НОВЫЙ)
   - Система избранного
   - Сравнение

5. **app/main.py** (ОБНОВИТЬ)
   - Подключить новый роутер favorites

6. **requirements.txt** (ДОБАВИТЬ)
   ```
   aiohttp>=3.9.0
   ```

---

## 🚀 Пошаговая миграция

### Шаг 1: Резервное копирование

```bash
# Создайте резервную копию текущей версии
cp -r app app.backup
cp requirements.txt requirements.txt.backup
cp -r alembic alembic.backup

# Создайте дамп базы данных
docker-compose exec db pg_dump -U postgres university_db > backup.sql
```

### Шаг 2: Обновление кода

```bash
# 1. Замените файлы из артефактов
#    - app/services/ai_service.py
#    - app/routers/ai.py
#    - app/routers/catalog.py
#    - app/main.py

# 2. Создайте новый файл
#    - app/routers/favorites.py

# 3. Обновите requirements.txt
echo "aiohttp>=3.9.0" >> requirements.txt
```

### Шаг 3: Установка зависимостей

**Вариант A: Docker (рекомендуется)**
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

**Вариант B: Без Docker**
```bash
pip install -r requirements.txt
```

### Шаг 4: Проверка миграций

```bash
# Проверьте текущее состояние
docker-compose exec backend alembic current

# Если нужно, примените миграции
docker-compose exec backend alembic upgrade head
```

### Шаг 5: Синхронизация AI базы

```bash
# Получите admin токен
ADMIN_TOKEN=$(curl -X POST "http://localhost:8080/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@university.kz&password=admin123" | jq -r '.access_token')

# Синхронизируйте векторную базу
curl -X POST "http://localhost:8080/ai/sync" \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### Шаг 6: Тестирование

```bash
# Проверьте здоровье системы
curl http://localhost:8080/health

# Проверьте документацию
open http://localhost:8080/docs

# Запустите базовые тесты
curl "http://localhost:8080/catalog/universities"
curl -X POST "http://localhost:8080/ai/chat" \
  -H "Content-Type: application/json" \
  -d '{"question": "Тест"}'
```

---

## 🔍 Проверка корректности миграции

### Контрольный список

- [ ] Сервер запускается без ошибок
- [ ] Документация открывается: `/docs`
- [ ] Каталог работает: `/catalog/universities`
- [ ] AI чат отвечает: `/ai/chat`
- [ ] Избранное доступно: `/favorites/my` (с токеном)
- [ ] Сравнение работает: `/favorites/compare`
- [ ] Нет ошибок в логах

### Команды проверки

```bash
# Проверка всех новых эндпоинтов
curl http://localhost:8080/catalog/universities
curl http://localhost:8080/catalog/stats
curl http://localhost:8080/catalog/cities

curl -X POST "http://localhost:8080/ai/chat" \
  -H "Content-Type: application/json" \
  -d '{"question": "Привет"}'

curl "http://localhost:8080/favorites/my" \
  -H "Authorization: Bearer TOKEN"

curl -X POST "http://localhost:8080/favorites/compare" \
  -H "Content-Type: application/json" \
  -d '{"university_ids": [1, 2]}'
```

---

## 🐛 Решение проблем

### Проблема 1: Сервер не запускается

**Симптомы:**
```
ModuleNotFoundError: No module named 'aiohttp'
```

**Решение:**
```bash
# Установите зависимости
pip install aiohttp

# Или пересоберите Docker
docker-compose build --no-cache
```

---

### Проблема 2: Ошибка импорта favorites

**Симптомы:**
```
ModuleNotFoundError: No module named 'app.routers.favorites'
```

**Решение:**
```bash
# Убедитесь, что файл существует
ls app/routers/favorites.py

# Если нет - создайте из артефакта
# Скопируйте содержимое из favorites_comparison артефакта
```

---

### Проблема 3: AI синхронизация не работает

**Симптомы:**
```json
{"error": "Failed to sync"}
```

**Решение:**
```bash
# 1. Проверьте OpenAI ключ
echo $OPENAI_API_KEY

# 2. Проверьте данные в БД
docker-compose exec backend python -c "
from app.db.database import AsyncSessionLocal
from app.db.models import University
from sqlalchemy import select
import asyncio

async def check():
    async with AsyncSessionLocal() as db:
        result = await db.execute(select(University))
        unis = result.scalars().all()
        print(f'Universities: {len(unis)}')

asyncio.run(check())
"

# 3. Загрузите тестовые данные если нужно
docker-compose exec backend python -m scripts.seed_data
```

---

### Проблема 4: Избранное не сохраняется

**Симптомы:**
```json
{"detail": "Невалидный токен"}
```

**Решение:**
```bash
# 1. Получите свежий токен
curl -X POST "http://localhost:8080/auth/login" \
  -d "username=student@gmail.com&password=student123"

# 2. Проверьте, что токен валиден
curl "http://localhost:8080/auth/me" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. Убедитесь, что таблица favorites существует
docker-compose exec db psql -U postgres -d university_db -c "\dt"
```

---

### Проблема 5: Каталог пустой

**Симптомы:**
```json
{"universities": [], "total": 0}
```

**Решение:**
```bash
# Загрузите тестовые данные
docker-compose exec backend python -m scripts.seed_data

# Или импортируйте JSON
docker-compose exec backend python -m scripts.import_json
```

---

### Проблема 6: Веб-поиск не работает

**Симптомы:**
```
used_web_search: false всегда
```

**Решение:**
```python
# В ai_service.py проверьте функцию _web_search
# Убедитесь, что aiohttp установлен
pip install aiohttp

# Для лучших результатов используйте SerpAPI
# Добавьте в .env:
SERPAPI_KEY=your_key_here
```

---

### Проблема 7: Ошибки в логах при AI запросах

**Симптомы:**
```
OpenAI API error: Rate limit exceeded
```

**Решение:**
```bash
# 1. Проверьте лимиты OpenAI
# 2. Добавьте задержки между запросами
# 3. Используйте кеширование

# В ai_service.py добавьте:
import time
time.sleep(1)  # Между запросами
```

---

## 🔄 Откат на предыдущую версию

Если что-то пошло не так:

```bash
# 1. Остановите контейнеры
docker-compose down

# 2. Восстановите файлы
cp -r app.backup/* app/
cp requirements.txt.backup requirements.txt
cp -r alembic.backup/* alembic/

# 3. Восстановите базу данных
docker-compose up -d db
docker-compose exec db psql -U postgres -d university_db < backup.sql

# 4. Запустите старую версию
docker-compose up -d
```

---

## 📊 Мониторинг после миграции

### Что проверять первые 24 часа:

```bash
# Логи ошибок
docker-compose logs -f backend | grep ERROR

# Производительность
curl -w "@curl-format.txt" "http://localhost:8080/catalog/universities"

# Где curl-format.txt:
time_namelookup:  %{time_namelookup}\n
time_connect:  %{time_connect}\n
time_total:  %{time_total}\n

# Использование памяти
docker stats backend

# Размер векторной базы
du -sh chroma_db/
```

---

## 🎯 Контрольные точки

### День 1
- [ ] Миграция завершена без ошибок
- [ ] Все тесты проходят
- [ ] Логи чистые

### Неделя 1
- [ ] Нет критических ошибок
- [ ] Производительность в норме
- [ ] Пользователи не жалуются

### Месяц 1
- [ ] AI рекомендации работают точно
- [ ] Избранное используется активно
- [ ] Сравнение помогает выбору

---

## 📞 Поддержка

При проблемах:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте документацию: `/docs`
3. Проверьте чек-лист тестирования
4. Откатитесь на backup если критично

---

## ✅ Успешная миграция

Признаки успешной миграции:

- ✅ Сервер работает стабильно
- ✅ Все эндпоинты отвечают
- ✅ AI чат работает и ищет в интернете при необходимости
- ✅ Каталог показывает университеты
- ✅ Избранное сохраняется
- ✅ Сравнение работает корректно
- ✅ Нет ошибок в логах
- ✅ Производительность в норме

---

**Версия:** 2.0.0  
**Дата:** Декабрь 2024  
**Время миграции:** ~15-30 минут  
**Сложность:** Средняя